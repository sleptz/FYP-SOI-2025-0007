apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s


    scrape_configs:
      - job_name: "kepler"
        static_configs:
          - targets:
              - kepler.kepler.svc.cluster.local:28282
